import 'package:flutter/widgets.dart';

class AppPaddings {
  AppPaddings._();

  static const EdgeInsets screen = EdgeInsets.all(16);
  static const EdgeInsets card = EdgeInsets.symmetric(horizontal: 16, vertical: 8);
}